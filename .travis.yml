language: python

python:
  - "3.6"

install:
  - pip install numpy
  - pip install pandas
  - pip install plotly

script:
  - python python/AtlantaRagnar2017.py

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
